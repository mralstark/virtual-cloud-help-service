# Pilot results

Status: awaiting 5–10 tester sessions. This template must not be filled with
synthetic data.

- Release commit:
- Node inventory timestamp:
- Test period (`first_test_at` -> `last_test_at`):
- Active users (`active_users`):
- Active devices (`active_devices`):
- Total tests (`total_tests`):
- AWG tests / successes / success rate:
- Reality tests / successes / success rate:
- Failures by stage (`failures_by_stage`):
- Failures by ISP, where voluntarily provided (`failures_by_isp`):
- Peak CPU / RAM / network from the private monitoring system:
- Total transfer from coarse server counters:
- Support incidents:
- Backup restore duration and result:

The admin report provides transport rates, optional ISP failure aggregates, active
devices, test period, and current node metrics when an observed node is configured.
Peak utilization and support incidents must come from the approved private
monitoring/incident records; do not infer them from a current sample.

## Recommendation

- Stay on one node or add a second:
- Primary transport and evidence:
- Need for operator-controlled fallback:
- Next provisioning step worth automating:
- Unresolved blocker/risk:

Do not include URLs, DNS history, traffic contents, client public IPs, connection
profiles, private keys, or subscriber identifiers.
